<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="1dp" /> <!-- Define el radio del borde -->
    <solid android:color="@android:color/transparent" /> <!-- Opcional: Define el color de fondo -->
    <stroke
        android:width="1px"
    android:color="@color/border" />
</shape>
